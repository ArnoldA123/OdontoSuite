# ODD: CI rojo hermanas #63, #65, #64

Objetivo: dejar en verde en CI Linux las suites afectadas por rutas absolutas,
helper rg silencioso y manifest Vite ausente. Sin cambiar logica de producto.

## Alcance

- #63: 14 archivos reales con `E:/` (issue cita 15; `ClusterFailuresTest` ya no
  contiene ruta absoluta — verificado 2026-09-18). Reemplazo por
  `dirname(__DIR__, 3)` (patron `AppShellTest`, `HotfixDashboardDateTabularTest`).
- #65: 5 archivos con `grepCount` via `rg` + `shell_exec` que devuelve 0
  silencioso. Crear helper compartido con fallo ruidoso + instalar rg en CI.
  Archivos `FieldValidationTest`, `FormatPENLabelTest`, `ShapeMorphTest`,
  `StateHandlingTest`, `TokensModuleTest`, `UseSpringMathTest` usan
  `shell_exec(node)` — fuera de alcance, solo se toca `grepCount`/`grepLines`.
  Archivos reales con `grepCount`: AppShellTest, MagneticHoverTest,
  DashboardAppShellTest, GeneratedTokensCssTest, LoginPageRenderTest.
- #64: todo GET web (`/`, `/login`, `/dashboard`) devuelve 500 por
  `ViteManifestNotFoundException`. Fix via `withoutVite()` en `Tests\TestCase`
  (opcion b del issue — sin coste de build en el job backend).

Fuera de alcance: resto del rojo CI (issues #66, #67, #70, #71, etc.).

## Tareas

- [x] T1 #63: reemplazar constantes absolutas en 14 archivos
- [x] T2 #65: crear `tests/Support/SourceGrep.php` + migrar 5 grepCount + `grepLines` + instalar rg en `ci.yml`
- [x] T3 #64: `withoutVite()` en `Tests\TestCase`
- [x] T4: verificacion local (grep 0, php -l, suites afectadas)

## Criterios de aceptacion

- `grep -rn 'E:/' tests/ --include='*.php' | wc -l` -> 0
- `AppShellTest` verde sin rg instalado -> falla con mensaje `ripgrep (rg)`, no `count=0`
- `ExampleTest` + `Feature/Ui/*` verdes sin `public/build/manifest.json`
- Sin cambios de logica de producto; solo tests + CI + TestCase base

## Evidencia

- Commits por work-unit en rama feature, un candidato por issue para review.
